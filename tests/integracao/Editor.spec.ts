describe('integração entre os componentes de TextoEditavel', () => {
    test.skip('ao adicionar um texto ele entra na lista de textos', async () => {
        // Testa a integração entre os componentes TextoEdicao -> AdicionarTexto -> ListaTextos
    });
    test.skip('ao editar um texto ele é alterado na lista de textos', async () => {
        // Testa a integração entre os componentes TextoEdicao -> AdicionarTexto -> ListaTextos
    });
    test.skip('ao cancelar a edição de um texto ele não é alterado', async () => {
        // Testa a integração entre os componentes TextoEdicao -> AdicionarTexto -> ListaTextos
    });
});

// fazer de todos os componentes da edição
